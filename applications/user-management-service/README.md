# user-management-service

## Purpose
Provides user identity, role management, and JWT issuance/validation support for platform services.

## Local run
1. Start dependencies and service:
   - `docker compose up --build`
2. Service endpoint:
   - `http://localhost:8081`

## Build and test
- `mvn clean verify`
