apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-management-service.fullname" . }}
  labels:
    {{- include "document-management-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "document-management-service.selectorLabels" . | nindent 4 }}