apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "document-review-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "document-review-service.labels" . | nindent 4 }}
    prometheus: kube-prometheus
spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      {{- include "document-review-service.selectorLabels" . | nindent 6 }}
  endpoints:
    - path: /actuator/prometheus
      interval: 15s
      targetPort: {{ .Values.service.targetPort }}
