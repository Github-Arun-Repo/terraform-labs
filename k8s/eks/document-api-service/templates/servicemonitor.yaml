apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "document-api-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "document-api-service.labels" . | nindent 4 }}
    prometheus: kube-prometheus
spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "document-api-service.name" . }}
  endpoints:
    - path: /actuator/prometheus
      interval: 15s
      targetPort: {{ .Values.service.targetPort }}
