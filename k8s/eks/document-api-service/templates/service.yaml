apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-api-service.fullname" . }}
  labels:
    {{- include "document-api-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    {{- include "document-api-service.selectorLabels" . | nindent 4 }}
