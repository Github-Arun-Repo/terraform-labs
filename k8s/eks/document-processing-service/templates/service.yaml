apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-processing-service.fullname" . }}
  labels:
    {{- include "document-processing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app.kubernetes.io/name: {{ include "document-processing-service.name" . }}
